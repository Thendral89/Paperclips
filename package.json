{
  "name": "paperclip-crm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply paperclip-crm --local",
    "migrate:remote": "wrangler d1 migrations apply paperclip-crm --remote"
  }
}
